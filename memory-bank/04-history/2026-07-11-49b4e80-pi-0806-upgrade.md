# 2026-07-11 - Troublemaker Pi 0.80.6 upgrade

Commits shipped:

- `troublemaker` `49b4e80690589c8fdba9da2c27f00d1a7d4cf821`
- Related Pi fork commits: `a515dbc2d0dca9d455f2b4f61f9b40d2327176f7`, `5b9f6df84226d1c0ddefcf6b4bee38b365114f01`, and `5a8e6a4f9d96c63b87011ad6575cabfd08542857`

Summary:

- Upgraded `@earendil-works/pi-agent-core`, `@earendil-works/pi-ai`, and `@earendil-works/pi-coding-agent` from 0.75.5 to 0.80.6.
- Migrated legacy `getModel` and `streamSimple` imports to Pi's documented `@earendil-works/pi-ai/compat` entrypoint.
- Updated the model-thinking test to use the same compatibility entrypoint.

Verification:

- `npm run typecheck`
- `npm run build`
- `npm run test:model-thinking`
- `npm run test:fireworks-model-resolution`
- `npm run test:model-config-current-selection`
- `npm run test:model-config-list-models`
- `npm run test:edge-runtime-import`
- `npm run validate:local` progressed through the upgrade-relevant tests, then stopped at the pre-existing stale `test:web-ui-settings-command-wiring` test because it references the June-deleted `ui/src/components/VoiceSettingsMenu.tsx`.

Deploy status:

- `main` pushed to `tinyfatco/troublemaker` and synchronized to `tiny-bat`.
- No legacy container image build or production deployment was performed.

Manual QA gaps:

- No live legacy agent was exercised because this was a maintenance-only dependency update and Flight remains the default runtime.
- The stale voice-settings UI tests remain outside this upgrade's scope.
